# Modelo MediaPipe

Coloque `gesture_recognizer.task` aqui (~10 MB).

```bash
# Na raiz do repositório
python scripts/download_model.py
copy models\gesture_recognizer.task apps\instrument\public\models\
```

Ou copie de `models/` após o download.
